{% extends "_layouts/cp" %}

{% set selectedSubnavItem = 'settings' %}

{% set title = "Venti Settings"|t %}

{% set tabs = {
	events: { label: "Events"|t, url: url('venti') },
	calendar: { label: "Calendar"|t, url: url('venti/calendar') },
	groups: { label: "Groups"|t, url: url('venti/groups') },
    settings: { label: "Settings"|t, url: url('venti/settings') }
} %}

{% set selectedTab = 'settings' %}

{# {%- set navItems = {
    'hd':           { heading: "Settings"|t },
    'general':      { title: "General Settings"|t },
    'events':       { title: "Event Settings"|t },
	'groups':       { title: "Group Settings"|t },
} -%} #}

{% set selectedItem = craft.app.request.getSegment(3)|default %}
{# {% set docTitle = navItems[selectedItem].title~' - '~title %}

{% block sidebar %}
    <nav>
        <ul>
            {% for id, item in navItems %}
                {% if item.heading is defined %}
                    <li class="heading"><span>{{ item.heading }}</span></li>
                {% else %}
                    <li><a href="{{ url('venti/settings/'~id) }}"{% if id == selectedItem %} class="sel"{% endif %}>{{ item.title }}</a></li>
                {% endif %}
            {% endfor %}
        </ul>
    </nav>
{% endblock %} #}
